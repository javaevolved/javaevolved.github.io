---
id: 18644872-5dff-481b-9c0f-a8399c09276a
slug: "collectors-flatmapping"
title: "Collectors.flatMapping()"
category: "streams"
difficulty: "intermediate"
jdkVersion: "9"
oldLabel: "Java 8"
modernLabel: "Java 9+"
oldApproach: "Nested flatMap"
modernApproach: "flatMapping()"
oldCode: |-
  // Flatten within a grouping collector
  // Required complex custom collector
  Map<String, Set<String>> tagsByDept =
      // no clean way in Java 8
modernCode: |-
  var tagsByDept = employees.stream()
      .collect(groupingBy(
          Emp::dept,
          flatMapping(
              e -> e.tags().stream(),
              toSet()
          )
      ));
summary: "Use flatMapping() to flatten inside a grouping collector."
explanation: "Collectors.flatMapping() applies a one-to-many mapping as a downstream\
  \ collector. It's the collector equivalent of Stream.flatMap() — useful inside groupingBy\
  \ or partitioningBy."
whyModernWins:
- icon: "🧩"
  title: "Composable"
  desc: "Works as a downstream collector inside groupingBy."
- icon: "📐"
  title: "One pass"
  desc: "Flatten and group in a single stream traversal."
- icon: "🔗"
  title: "Nestable"
  desc: "Combine with other downstream collectors."
support:
  state: "available"
  description: "Widely available since JDK 9 (Sept 2017)"
prev: "streams/stream-takewhile-dropwhile"
next: "streams/stream-tolist"
related:
- "streams/optional-ifpresentorelse"
- "streams/stream-of-nullable"
- "streams/stream-iterate-predicate"
tags:
- streams
- collectors
docs:
- title: "Collectors.flatMapping()"
  href: "https://docs.oracle.com/en/java/javase/25/docs/api/java.base/java/util/stream/Collectors.html#flatMapping(java.util.function.Function,java.util.stream.Collector)"
