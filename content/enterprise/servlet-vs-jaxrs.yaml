---
id: 98
slug: "servlet-vs-jaxrs"
title: "Servlet versus JAX-RS"
category: "enterprise"
difficulty: "intermediate"
jdkVersion: "11"
oldLabel: "Java EE"
modernLabel: "Jakarta EE 8+"
oldApproach: "HttpServlet"
modernApproach: "JAX-RS Resource"
oldCode: |-
  @WebServlet("/users")
  public class UserServlet extends HttpServlet {
      @Override
      protected void doGet(HttpServletRequest req,
                           HttpServletResponse res)
              throws ServletException, IOException {
          String id = req.getParameter("id");
          res.setContentType("application/json");
          res.getWriter().write("{\"id\":\"" + id + "\"}");
      }
  }
modernCode: |-
  @Path("/users")
  public class UserResource {
      @GET
      @Produces(MediaType.APPLICATION_JSON)
      public Response getUser(
              @QueryParam("id") String id) {
          return Response.ok(new User(id)).build();
      }
  }
summary: "Replace verbose HttpServlet boilerplate with declarative JAX-RS resource\
  \ classes."
explanation: "JAX-RS (Jakarta RESTful Web Services) lets you expose REST endpoints\
  \ using simple annotations like @GET, @Path, and @Produces. No more manual parsing\
  \ of request parameters or setting content types on the response — the runtime handles\
  \ marshalling and routing automatically."
whyModernWins:
- icon: "📐"
  title: "Declarative routing"
  desc: "Annotations define HTTP method, path, and content type instead of imperative\
    \ if/else dispatch."
- icon: "🔄"
  title: "Automatic marshalling"
  desc: "Return POJOs directly; the runtime serialises them to JSON or XML based on\
    \ @Produces."
- icon: "🧪"
  title: "Easier testing"
  desc: "Resource classes are plain Java objects, testable without a servlet container."
support:
  state: "available"
  description: "Widely available since Jakarta EE 8 / Java 11"
prev: "language/compact-canonical-constructor"
next: "enterprise/ejb-vs-cdi"
related:
- "enterprise/ejb-vs-cdi"
- "enterprise/jdbc-vs-jpa"
- "io/http-client"
docs:
- title: "Jakarta RESTful Web Services Specification"
  href: "https://jakarta.ee/specifications/restful-ws/"
- title: "Jakarta REST 3.1 API"
  href: "https://jakarta.ee/specifications/restful-ws/3.1/apidocs/"
