---
id: 16
slug: "primitive-types-in-patterns"
title: "Primitive types in patterns"
category: "language"
difficulty: "advanced"
jdkVersion: "25"
oldLabel: "Java 8"
modernLabel: "Java 25 (Preview)"
oldApproach: "Manual Range Checks"
modernApproach: "Primitive Patterns"
oldCode: |-
  String classify(int code) {
      if (code >= 200 && code < 300)
          return "success";
      else if (code >= 400 && code < 500)
          return "client error";
      else
          return "other";
  }
modernCode: |-
  String classify(int code) {
      return switch (code) {
          case int c when c >= 200
              && c < 300 -> "success";
          case int c when c >= 400
              && c < 500 -> "client error";
          default -> "other";
      };
  }
summary: "Pattern matching now works with primitive types, not just objects."
explanation: "Java 25 extends pattern matching to primitive types. You can use int,\
  \ long, double etc. in switch patterns with when guards, eliminating the need for\
  \ boxing or manual range checks."
whyModernWins:
- icon: "📦"
  title: "No boxing"
  desc: "Match primitives directly — no Integer wrapper needed."
- icon: "🎯"
  title: "Pattern consistency"
  desc: "Same pattern syntax for objects and primitives."
- icon: "⚡"
  title: "Better performance"
  desc: "Avoid autoboxing overhead in pattern matching."
support:
  state: "preview"
  description: "Preview in JDK 25 (third preview, JEP 507). Requires --enable-preview."
prev: "language/guarded-patterns"
next: "language/module-import-declarations"
related:
- "language/type-inference-with-var"
- "language/text-blocks-for-multiline-strings"
- "language/switch-expressions"
docs:
- title: "Primitive Types in Patterns (JEP 488)"
  href: "https://openjdk.org/jeps/488"
