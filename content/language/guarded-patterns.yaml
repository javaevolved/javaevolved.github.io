---
id: a0a359b3-2d7e-4bc9-9e98-72fdb1287c89
slug: "guarded-patterns"
title: "Guarded patterns with when"
category: "language"
navigationOrder: 15000
difficulty: "intermediate"
jdkVersion: "21"
oldLabel: "Java 8"
modernLabel: "Java 21+"
oldApproach: "Nested if"
modernApproach: "when Clause"
oldCode: |-
  if (shape instanceof Circle) {
      Circle c = (Circle) shape;
      if (c.radius() > 10) {
          return "large circle";
      } else {
          return "small circle";
      }
  } else {
      return "not a circle";
  }
modernCode: |-
  return switch (shape) {
      case Circle c
          when c.radius() > 10
              -> "large circle";
      case Circle c
              -> "small circle";
      default -> "not a circle";
  };
summary: "Add conditions to pattern cases using when guards."
explanation: "Guarded patterns let you refine a type match with an additional boolean\
  \ condition. This keeps all the branching logic in the switch instead of nesting\
  \ if statements inside cases."
whyModernWins:
- icon: "🎯"
  title: "Precise matching"
  desc: "Combine type + condition in a single case label."
- icon: "📐"
  title: "Flat structure"
  desc: "No nested if/else inside switch cases."
- icon: "📖"
  title: "Readable intent"
  desc: "The when clause reads like natural language."
support:
  state: "available"
  description: "Widely available since JDK 21 LTS (Sept 2023)"
related:
- "language/record-patterns"
- "language/type-inference-with-var"
- "language/compact-source-files"
tags:
- pattern-matching
- switch
docs:
- title: "Pattern Matching for switch (JEP 441)"
  href: "https://openjdk.org/jeps/441"
